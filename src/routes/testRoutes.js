// import express from "express";
// import {
//   clearCustomCookie,
//   clearDefaultTokenCookie,
//   readCookies,
//   setCustomCookie,
//   setDefaultTokenCookie,
// } from "../controllers/test.js";

// const router = express.Router();

// router.post("/cookie/token", setDefaultTokenCookie);
// router.post("/cookie/custom", setCustomCookie);
// router.get("/cookie", readCookies);
// router.delete("/cookie/token", clearDefaultTokenCookie);
// router.delete("/cookie/custom", clearCustomCookie);

// export default router;
